{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "提交tag触发发布",
            "type": "shell",
            "command": "bump patch && git push origin --all && git push origin --tags",
            "problemMatcher": []
        }
    ]
}